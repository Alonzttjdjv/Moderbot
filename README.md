# 🤖 Telegram Chat Moderator Bot

Мощный и многофункциональный бот-модератор для Telegram чатов, построенный на aiogram 3.4.1 с расширенными возможностями автоматической модерации.

## ✨ Основные возможности

### 🛡️ Автоматическая модерация
- **Защита от спама** - автоматическое обнаружение и удаление спам-сообщений
- **Антифлуд** - предотвращение слишком частых сообщений от одного пользователя
- **Фильтрация контента** - проверка на запрещенные слова, ссылки, капс, эмодзи-спам
- **Умная система предупреждений** - автоматическое начисление предупреждений

### 🔧 Система модерации
- **Предупреждения** - система предупреждений с настраиваемыми лимитами
- **Временные муты** - ограничение отправки сообщений на заданное время
- **Временные баны** - блокировка пользователей с автоматическим разбаном
- **Логирование действий** - полная история всех действий модерации

### 📊 Статистика и аналитика
- **Детальная статистика чата** - количество сообщений, пользователей, активности
- **Статистика модерации** - количество предупреждений, мутов, банов
- **Анализ активности** - отслеживание активности пользователей
- **Экспорт данных** - возможность экспорта статистики в различных форматах

### 🔍 Настраиваемые фильтры
- **Регулярные выражения** - гибкая настройка фильтров контента
- **Различные типы фильтров** - слова, ссылки, капс, эмодзи, длина сообщений
- **Автоматические действия** - настраиваемые реакции на нарушения
- **Тестирование фильтров** - проверка работы фильтров на тестовом тексте

### 💾 Управление данными
- **SQLite база данных** - надежное хранение всех данных
- **Автоматические резервные копии** - создание и управление бэкапами
- **Восстановление данных** - возможность восстановления из резервных копий
- **Очистка старых данных** - автоматическое удаление устаревшей информации

## 🚀 Установка и настройка

### Требования
- Python 3.8+
- aiogram 3.4.1
- python-dotenv
- aiofiles
- sqlite3

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd telegram-chat-moderator-bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации
Скопируйте файл `.env.example` в `.env` и заполните необходимые параметры:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
# Токен вашего бота (получите у @BotFather)
BOT_TOKEN=your_bot_token_here

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# Путь к базе данных
DATABASE_PATH=./data/bot_database.db

# Уровень логирования
LOG_LEVEL=INFO
```

### 4. Создание необходимых директорий
```bash
mkdir -p data backups logs
```

### 5. Запуск бота
```bash
python main.py
```

## 📋 Команды бота

### Основные команды
- `/start` - Запуск бота и основная информация
- `/help` - Справка по командам
- `/settings` - Настройки модерации
- `/stats` - Статистика чата
- `/filters` - Управление фильтрами
- `/moderate` - Панель модерации

### Команды модерации
- `/warn @username [причина]` - Выдать предупреждение
- `/mute @username [время] [причина]` - Выдать мут
- `/ban @username [время] [причина]` - Выдать бан
- `/unban @username` - Разбанить пользователя
- `/unmute @username` - Размутить пользователя

### Команды управления фильтрами
- `/addfilter` - Добавить новый фильтр
- `/delfilter` - Удалить фильтр
- `/listfilters` - Список всех фильтров
- `/testfilter` - Протестировать фильтр

## ⚙️ Конфигурация

### Основные настройки модерации
```python
# config.py

# Максимальное количество предупреждений
MAX_WARNINGS = 3

# Автоматическое удаление спама
AUTO_DELETE_SPAM = True

# Порог для определения спама
SPAM_THRESHOLD = 5

# Защита от флуда
FLOOD_PROTECTION = True
```

### Фильтры контента
```python
CONTENT_FILTERS = {
    'bad_words': True,        # Запрещенные слова
    'links': True,            # Ссылки
    'caps': True,             # Капс
    'emoji_spam': True,       # Спам эмодзи
    'long_messages': True,    # Длинные сообщения
    'repeated_chars': True,   # Повторяющиеся символы
    'number_spam': True,      # Спам числами
    'max_message_length': 1000 # Максимальная длина сообщения
}
```

### Длительности временных банов
```python
TEMP_BAN_DURATIONS = {
    'warning': 0,      # Предупреждение (без ограничений)
    'mute_1': 3600,   # Мут на 1 час
    'mute_2': 7200,   # Мут на 2 часа
    'ban_1': 86400,   # Бан на 1 день
    'ban_2': 604800   # Бан на 1 неделю
}
```

## 🗄️ Структура базы данных

Бот использует SQLite базу данных со следующими таблицами:

- **chats** - информация о чатах и их настройках
- **users** - данные пользователей и их статусы
- **moderation_actions** - история действий модерации
- **messages** - логи сообщений
- **statistics** - статистика чатов
- **filters** - настраиваемые фильтры
- **temp_bans** - временные баны и муты

## 🔧 Архитектура проекта

```
telegram-chat-moderator-bot/
├── main.py              # Основной файл бота
├── config.py            # Конфигурация
├── database.py          # Управление базой данных
├── moderator.py         # Логика модерации
├── filters.py           # Система фильтров
├── utils.py             # Утилиты
├── file_utils.py        # Управление файлами
├── requirements.txt     # Зависимости
├── .env.example        # Пример конфигурации
├── data/               # База данных
├── backups/            # Резервные копии
└── logs/               # Логи
```

## 🚀 Развертывание

### Локальный запуск
```bash
python main.py
```

### Запуск в фоне (Linux/Mac)
```bash
nohup python main.py > bot.log 2>&1 &
```

### Использование systemd (Linux)
Создайте файл `/etc/systemd/system/telegram-moderator-bot.service`:

```ini
[Unit]
Description=Telegram Chat Moderator Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable telegram-moderator-bot
sudo systemctl start telegram-moderator-bot
```

### Docker (опционально)
```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

## 📊 Мониторинг и логирование

### Логирование
Бот ведет подробные логи в файл `bot.log` и выводит их в консоль. Уровень логирования настраивается в конфигурации.

### Мониторинг
- Автоматическая проверка состояния бота
- Мониторинг использования диска
- Отслеживание производительности
- Уведомления об ошибках

### Резервное копирование
- Автоматическое создание резервных копий
- Настраиваемое расписание
- Управление хранением бэкапов
- Возможность восстановления

## 🔒 Безопасность

### Права доступа
- Проверка прав администратора для всех критических операций
- Валидация входных данных
- Защита от SQL-инъекций
- Ограничение частоты запросов

### Конфиденциальность
- Шифрование чувствительных данных
- Безопасное хранение токенов
- Логирование действий для аудита
- Возможность удаления данных пользователей

## 🆘 Поддержка и устранение неполадок

### Частые проблемы

#### Бот не отвечает
1. Проверьте токен бота в `.env` файле
2. Убедитесь, что бот добавлен в чат
3. Проверьте права бота в чате

#### Ошибки базы данных
1. Проверьте права доступа к директории `data/`
2. Убедитесь, что SQLite установлен
3. Проверьте целостность файла базы данных

#### Проблемы с модерацией
1. Проверьте настройки фильтров
2. Убедитесь, что бот имеет права администратора
3. Проверьте логи на наличие ошибок

### Логи и отладка
Включите подробное логирование, изменив `LOG_LEVEL=DEBUG` в `.env` файле.

### Получение помощи
- Создайте issue в репозитории
- Опишите проблему подробно
- Приложите логи и конфигурацию

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! 

### Как помочь
1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

### Что можно улучшить
- Добавление новых типов фильтров
- Улучшение алгоритмов обнаружения спама
- Расширение статистики
- Создание веб-интерфейса
- Добавление поддержки других баз данных

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🙏 Благодарности

- Команде aiogram за отличную библиотеку
- Сообществу Python за инструменты и библиотеки
- Всем, кто тестировал и улучшал бота

---

**Telegram Chat Moderator Bot** - профессиональное решение для модерации ваших Telegram чатов! 🚀